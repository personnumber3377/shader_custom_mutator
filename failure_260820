#version 300 es
precision mediump float;
struct NestedUniforms {
    float x[3];
};
struct Uniforms {
    NestedUniforms a;
    float b;
    float c;
    float[5] d;
};

struct S {
  float a;
};

out vec4 color;

uniform struct T {
  float f;
} x;

float f(float None, out vec2 o1, out S o2[2], out float o3[3])
{
  float uninitialized_local = 1.0;
  o2[0].a = None;
  o3[1] = 0.5;
  return uninitialized_local;
}

void main()
{
  S s_1067 = S(-1.0);
  float v0 = 345.0;
  vec2 v1 = vec2(123.0, 234.0);
  S v2[2] = S[2](S(-1111.0), S(55.0));
  S s_9093 = S(+(-2.0 * +1.0));
  float v3[3] = float[3](20.0, 30.0, 40.0);
  S s_2551 = S(+2.0);
  float v4 = f(v0, v1, v2, v3);
  color = vec4((((v0 + v1.x) + v2[0].a) + v3[0]), ((v1.y + v2[1].a) + v3[1]), (v3[2] + v4), 1.0);
struct T {
  float q;
};

  T s_1725 = T(+-1.0);
  vec4(2.0, -1.0, -0.0, 1.0);
}





Expected ID  at 143, got [:[