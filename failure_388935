precision mediump float;
precision mediump int;

float fun(float arg)
{
  return pow(arg, -4.0);
}

void main()
{
  const float a = 1e-06;
  float b = fun(a);
  if ((abs((1e+24 - b)) < 1e+21))
  {
    gl_FragColor = vec4(0.0, 1.0, 0.0, 1.0);
  }
  else
  {
    gl_FragColor = vec4(1.0, 0.0, 0.0, 1.0);
  }
}





Expected ;  at 133, got ID:e