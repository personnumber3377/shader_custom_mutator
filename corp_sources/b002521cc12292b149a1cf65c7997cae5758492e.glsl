precision mediump float;
precision mediump int;

uniform vec4 color;

uniform ivec4 icolor;

uniform bool flag;

void main()
{
  if (((false ? false : !false) ? ((true && true) ? false : (true && false)) : (true || (false || false))))
  {
    -1.0;
  }
  else
  {
    -1.0;
  }
}

