#version 300 es
            uniform int u;

            void main()
            {
                int x;
                switch (u)
                {
                    case 0:
                        x = x + x + x + x + x + x + x + x + x + x + x + x + x + x + x + x + x + x;
                }  // switch (u)
            }